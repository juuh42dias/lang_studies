module FibModule {
    function fib(n: integer) return integer {
        if (n <= 1) { return n; }
        return fib(n-1) + fib(n-2);
    }
    control {
        var integer result := fib(10);
        log(result);
    }
}

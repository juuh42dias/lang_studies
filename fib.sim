begin
    integer procedure fib(n); integer n;
    begin
        if n <= 1 then
            fib := n
        else
            fib := fib(n-1) + fib(n-2)
    end;
    outint(fib(10), 1);
    outimage
end
